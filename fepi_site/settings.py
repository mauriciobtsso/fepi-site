"""
Django settings for fepi_site project.
Generated by 'django-admin startproject' using Django 5.2.9.
"""

from pathlib import Path
import os
import dj_database_url

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# SECRET_KEY
# Tenta pegar do ambiente (Railway) ou usa uma padrão insegura para local
SECRET_KEY = os.environ.get('SECRET_KEY', 'django-insecure-#up2mw6n4z7=cl@*$c$y@!!la*(*-!idc+0y_dabnogc$2zjj(')

# --------------------------------------------------------
# 🚀 Configuração Dinâmica de Ambientes
# --------------------------------------------------------

# Verifica se estamos no Railway
IS_RAILWAY_PROD = 'DATABASE_URL' in os.environ

if IS_RAILWAY_PROD:
    # MODO PRODUÇÃO (RAILWAY)
    DEBUG = False
    
    ALLOWED_HOSTS = ['*'] # Aceita o domínio do Railway automaticamente
    
    # Conexão com o Banco de Dados do Railway
    DATABASES = {
        'default': dj_database_url.config(
            default=os.environ.get('DATABASE_URL'),
            conn_max_age=600,
            conn_health_checks=True,
        )
    }

    # Segurança HTTPS
    SECURE_PROXY_SSL_HEADER = ('HTTP_X_FORWARDED_PROTO', 'https')
    SECURE_SSL_REDIRECT = True
    SESSION_COOKIE_SECURE = True
    CSRF_COOKIE_SECURE = True
    # Adiciona os domínios confiáveis (Railway e o antigo PythonAnywhere se necessário)
    CSRF_TRUSTED_ORIGINS = ['https://*.railway.app', 'https://mauriciobts.pythonanywhere.com']

else:
    # MODO DESENVOLVIMENTO (Local - SQLite)
    DEBUG = True 
    ALLOWED_HOSTS = ['*'] 
    
    DATABASES = {
        'default': {
            'ENGINE': 'django.db.backends.sqlite3',
            'NAME': BASE_DIR / 'db.sqlite3',
        }
    }


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    # CLOUDINARY (Deve vir ANTES de staticfiles)
    'cloudinary_storage',
    'django.contrib.staticfiles',
    'cloudinary',
    'django.contrib.humanize',
    # Minhas Apps
    'core',
    'livraria',
    'noticias',
    'centros',
    'programacao',
    'recursos',
    'doacoes',
    'intranet', 
    # Libs
    'ckeditor',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'whitenoise.middleware.WhiteNoiseMiddleware', # WhiteNoise para CSS
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'fepi_site.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [BASE_DIR / 'core' / 'templates'], 
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                'core.context_processors.site_wide_context',
            ],
        },
    },
]

WSGI_APPLICATION = 'fepi_site.wsgi.application'


# Password validation
AUTH_PASSWORD_VALIDATORS = [
    {'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator'},
    {'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator'},
    {'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator'},
    {'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator'},
]


# Internationalization
LANGUAGE_CODE = 'pt-br'
TIME_ZONE = 'America/Sao_Paulo'
USE_I18N = True
USE_TZ = True


# --- ARQUIVOS ESTÁTICOS (CSS, JS) ---
# Geridos pelo WhiteNoise (Ficam no servidor, são leves)
STATIC_URL = '/static/'
STATIC_ROOT = BASE_DIR / 'staticfiles' 
STATICFILES_DIRS = [
    BASE_DIR / 'static',
]

# --- ARQUIVOS DE MÍDIA (FOTOS, PDFs) ---
# Geridos pelo Cloudinary (Armazenamento Externo Gratuito)
MEDIA_URL = '/media/' 
MEDIA_ROOT = BASE_DIR / 'media' 

# Configuração Limpa do Cloudinary (Sem proxy!)
CLOUDINARY_STORAGE = {
    'CLOUD_NAME': 'dym1yoj68',
    'API_KEY': '283348431723888',
    'API_SECRET': 'PRLSa_vmaDRFTE0TJnDBsec-N24',
}

# --- CONFIGURAÇÃO CKEDITOR ---
# Pasta onde os uploads feitos pelo editor de texto vão ficar
CKEDITOR_UPLOAD_PATH = "uploads/"

# Opcional: Garante que as imagens aparecem logo após o upload
CKEDITOR_IMAGE_BACKEND = "pillow"

# Configuração básica da barra de ferramentas (opcional, mas útil)
CKEDITOR_CONFIGS = {
    'default': {
        'toolbar': 'Custom',
        'toolbar_Custom': [
            ['Bold', 'Italic', 'Underline'],
            ['NumberedList', 'BulletedList', '-', 'Outdent', 'Indent', '-', 'JustifyLeft', 'JustifyCenter', 'JustifyRight', 'JustifyBlock'],
            ['Link', 'Unlink'],
            ['RemoveFormat', 'Source', 'Image', 'Table']
        ],
        'width': 'auto',
    },
}

# Configuração de Armazenamento para Django 5
STORAGES = {
    # Default: Usa Cloudinary para uploads (Imagens, PDFs da Intranet)
    "default": {
        "BACKEND": "cloudinary_storage.storage.MediaCloudinaryStorage",
    },
    # Staticfiles: Usa WhiteNoise com compressão (CSS/JS)
    "staticfiles": {
        "BACKEND": "whitenoise.storage.CompressedStaticFilesStorage",
    },
}

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'
SECURE_REFERRER_POLICY = "strict-origin-when-cross-origin"

# Email
EMAIL_RECEIVER = 'contato@fepiaui.org.br' 
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = os.environ.get('EMAIL_HOST_USER', 'seu_email_remetente@gmail.com') 
EMAIL_HOST_PASSWORD = os.environ.get('EMAIL_HOST_PASSWORD', 'sua_senha_de_app_aqui') 
DEFAULT_FROM_EMAIL = EMAIL_HOST_USER

# Login
LOGIN_URL = 'login'               
LOGIN_REDIRECT_URL = 'area_federado' 
LOGOUT_REDIRECT_URL = 'home'
