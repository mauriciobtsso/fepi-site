"""
Django settings for fepi_site project.

Generated by 'django-admin startproject' using Django 5.2.9.
"""

from pathlib import Path
import os
import dj_database_url

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/5.2/howto/deployment/checklist/

# SECRET_KEY: Tenta obter a chave do ambiente (Railway) ou usa o fallback (Local/PythonAnywhere)
# IMPORTANTE: No Railway, adicione uma chave forte e nova na variável SECRET_KEY!
SECRET_KEY = os.environ.get('SECRET_KEY', 'django-insecure-#up2mw6n4z7=cl@*$c$y@!!la*(*-!idc+0y_dabnogc$2zjj(')


# --------------------------------------------------------
# 🚀 Configuração Dinâmica de Ambientes (DEBUG, HOSTS, DB)
# --------------------------------------------------------

# Verifica se a variável de ambiente DATABASE_URL existe (só existirá no Railway)
IS_RAILWAY_PROD = 'DATABASE_URL' in os.environ

if IS_RAILWAY_PROD:
    # MODO PRODUÇÃO (RAILWAY - PostgreSQL)
    DEBUG = False
    
    # Importante: O .railway.app é para o domínio de deploy
    # Substitua 'seusitedjango.railway.app' pelo domínio real.
    ALLOWED_HOSTS = [
        '127.0.0.1', 
        '.railway.app', 
        'localhost', 
        'seusitedjango.railway.app', 
        # Adicione o domínio do seu PythonAnywhere aqui se for específico:
        # 'seudominio.pythonanywhere.com' 
    ]
    
    # DATABASES: Usa PostgreSQL
    DATABASES = {
        'default': dj_database_url.config(
            default=os.environ.get('DATABASE_URL'),
            conn_max_age=600,
            conn_health_check=True,
        )
    }

else:
    # MODO DESENVOLVIMENTO/STAGING (Local/PythonAnywhere - SQLite)
    DEBUG = True 
    
    # Configuração permissiva para desenvolvimento/staging
    ALLOWED_HOSTS = ['*'] 
    
    # DATABASES: Usa SQLite
    DATABASES = {
        'default': {
            'ENGINE': 'django.db.backends.sqlite3',
            'NAME': BASE_DIR / 'db.sqlite3',
        }
    }


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    # Minhas Apps
    'core',
    'livraria',
    'noticias',
    'centros',
    'programacao',
    'recursos',
    'doacoes',
]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'fepi_site.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                'core.context_processors.site_wide_context',
            ],
        },
    },
]

WSGI_APPLICATION = 'fepi_site.wsgi.application'


# Password validation
# https://docs.djangoproject.com/en/5.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/5.2/topics/i18n/

LANGUAGE_CODE = 'pt-br'

TIME_ZONE = 'America/Sao_Paulo'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/5.2/howto/static-files/

STATIC_URL = '/static/'

# Onde o 'collectstatic' irá COPIAR TODOS os arquivos estáticos (CRUCIAL para Railway)
# Usando Pathlib para ser compatível com a forma como BASE_DIR foi definida
STATIC_ROOT = BASE_DIR / 'staticfiles' 

# Onde o Django procura arquivos estáticos extras no teu computador (além das pastas 'static' dos apps)
# Usando Pathlib para ser compatível com a forma como BASE_DIR foi definida
STATICFILES_DIRS = [
    BASE_DIR / 'static',
]

# Configuração de Arquivos de Media (Imagens, Uploads)
MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'

# Default primary key field type
DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# Permite que o YouTube saiba que o vídeo está no teu site (Corrige Erro 153)
SECURE_REFERRER_POLICY = "strict-origin-when-cross-origin"

# ----------------------------------------------------
# CONFIGURAÇÃO DE EMAIL (Para que o formulário funcione)
# ----------------------------------------------------

# ENDEREÇO PARA ONDE OS EMAILS SERÃO ENVIADOS
EMAIL_RECEIVER = 'contato@fepiaui.org.br' 

# CONFIGURAÇÕES DO SERVIDOR SMTP 
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
# Usa variáveis de ambiente para credenciais de email em produção
EMAIL_HOST_USER = os.environ.get('EMAIL_HOST_USER', 'seu_email_remetente@gmail.com') 
EMAIL_HOST_PASSWORD = os.environ.get('EMAIL_HOST_PASSWORD', 'sua_senha_de_app_aqui') 
DEFAULT_FROM_EMAIL = EMAIL_HOST_USER